package com.distributed.ledger.domain.port.in;

/**
 * Marker interface for all inbound ports (use cases).
 * These define what the application can do.
 * Implementations live in the application layer.
 */
public interface UseCase {
}


